// Learning Processing
// Daniel Shiffman
// http://www.learningprocessing.com

// Exercise 5-7: Why doesn't the following code work 
// properly when it is moved to draw()?

/*
if (mouseX > x && mouseX < x+w && mouseY > y && mouseY < y+h && mousePressed) {
  button = !button;
}
*/
